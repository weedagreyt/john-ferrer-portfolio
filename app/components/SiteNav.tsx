"use client";
import {useState} from "react";import {usePathname} from "next/navigation";import styles from "./site-nav.module.css";import {siteAssets} from "../lib/portfolio";
export default function SiteNav({theme="light"}:{theme?:"light"|"dark"}){const pathname=usePathname();const[open,setOpen]=useState(false);const active=(h:string)=>h==="/"?pathname==="/":pathname.startsWith(h);return <header className={`${styles.header} ${styles[theme]}`}><div className={styles.inner}><a className={styles.brand} href="/" aria-label="John Ferrer home"><img src={siteAssets.logo} alt="John Ferrer logo"/></a><nav className={styles.desktopNav}><div className={styles.navLinks}><a className={active('/work')?styles.active:''} href="/work">Work</a><a className={active('/about')?styles.active:''} href="/about">About</a><a href="/#work">Journal</a><a className={active('/contact')?styles.active:''} href="/contact">Contact</a></div><a className={styles.contact} href="/contact">Let’s Talk <span>→</span></a></nav><button className={styles.menuButton} type="button" aria-label={open?'Close menu':'Open menu'} onClick={()=>setOpen(!open)}><span/><span/></button></div><div className={`${styles.mobileMenu} ${open?styles.mobileOpen:''}`}><a href="/work">Work</a><a href="/about">About</a><a href="/#work">Journal</a><a href="/contact">Contact</a><a className={styles.mobileContact} href="/contact">Let’s Talk</a></div></header>}
